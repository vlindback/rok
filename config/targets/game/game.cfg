# DLL stems — the host resolves the full path based on platform and build profile.
# Windows:  .build/{profile}/{stem}.dll
# Linux:    .build/{profile}/lib{stem}.so
# macOS:    .build/{profile}/lib{stem}.dylib
engine = rok_engine
target = rok_game